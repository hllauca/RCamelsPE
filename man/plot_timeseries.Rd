% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_timeseries}
\alias{plot_timeseries}
\title{Plot CAMELS-PE Time Series}
\usage{
plot_timeseries(
  data,
  variable = "flow_obs",
  gauge_id = NULL,
  date_col = "date",
  facet = TRUE,
  scales = "free_y",
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing CAMELS-PE time series.}

\item{variable}{Character string. Name of the variable to plot.}

\item{gauge_id}{Optional character vector. Gauge IDs used to filter the data
before plotting. If \code{NULL}, all available gauges are plotted.}

\item{date_col}{Character string. Name of the date column.}

\item{facet}{Logical value. If \code{TRUE}, creates one panel per gauge ID.}

\item{scales}{Character string. Scales passed to \code{ggplot2::facet_wrap()}.
One of \code{"fixed"}, \code{"free"}, \code{"free_x"}, or \code{"free_y"}.}

\item{...}{Additional arguments passed to \code{ggplot2::geom_line()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a time series plot for one CAMELS-PE variable, such as precipitation,
observed streamflow, simulated streamflow, or temperature. The function
returns a \code{ggplot} object, so it can be further customized using standard
\code{ggplot2} layers.
}
\details{
Use \code{read_dictionary(category = "timeseries")} to inspect available
time series variables, descriptions, units, and data sources.
}
\examples{
path <- system.file(
  "extdata",
  "sample_camels_pe",
  package = "RCamelsPE"
)

# Inspect available time series variables
read_dictionary(
  category = "timeseries",
  path = path
)

ts <- read_timeseries(
  gauge_id = c("PE_212900", "PE_200907"),
  vars = c(
    "date",
    "gauge_id",
    "prec",
    "flow_obs",
    "flow_sim",
    "tmean"
  ),
  path = path
)

plot_timeseries(ts, variable = "flow_obs")
plot_timeseries(ts, variable = "prec", linewidth = 0.4)
plot_timeseries(ts, variable = "flow_obs", facet = FALSE)
plot_timeseries(ts, variable = "flow_obs", gauge_id = "PE_212900")
}
