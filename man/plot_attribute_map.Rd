% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_attribute_map}
\alias{plot_attribute_map}
\title{Plot CAMELS-PE Attribute Map}
\usage{
plot_attribute_map(
  catchments,
  attributes,
  variable,
  gauges = NULL,
  na_color = "grey80",
  ...
)
}
\arguments{
\item{catchments}{An \code{sf} object with catchment polygons.}

\item{attributes}{A data frame or tibble with CAMELS-PE attributes.}

\item{variable}{Character string. Name of the attribute to visualize.}

\item{gauges}{Optional \code{sf} object with gauge point locations.}

\item{na_color}{Character string. Color for missing values.}

\item{...}{Additional arguments passed to \code{ggplot2::geom_sf()} for
catchments.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a thematic map of CAMELS-PE catchments using a selected attribute.
The function joins attribute data with catchment geometries by
\code{gauge_id} and returns a \code{ggplot} object.
}
\details{
Use \code{read_dictionary()} to inspect available attributes,
descriptions, units, categories, source files, and data sources.
}
\examples{
path <- system.file(
  "extdata",
  "sample_camels_pe",
  package = "RCamelsPE"
)

# Inspect available topographic attributes
read_dictionary(
  category = "topographic",
  path = path
)

catchments <- read_geospatial(
  type = "catchments",
  path = path
)

gauges <- read_geospatial(
  type = "gauges",
  path = path
)

attr <- read_attributes(
  type = "topographic",
  path = path
)

plot_attribute_map(
  catchments = catchments,
  attributes = attr,
  variable = "area",
  gauges = gauges
)
}
